# 策略库说明

本项目内置 **9 种预设策略**（定义在 `backend/routers/strategy.py` 的 `PRESETS`），覆盖趋势跟踪、均值回归、超买超卖、箱体震荡、回调低吸、超跌反弹等场景。

在这 9 个策略之上还有一层**选股系统**（`backend/services/market_select.py`）：它先判断大盘环境，再按「分轨制」从全市场筛出当天值得关注的标的，并指定该用哪条策略去交易。选股的完整规则见文末 **[《行情选股》](#行情选股选股系统)**。

所有策略由回测引擎 `backend/services/backtest_engine.py` 实现（对应各自的 `signals_*` 方法），信号基于**日线 K 线**计算。信号值是**目标仓位比例**，不是布尔标记：

| 信号值 | 含义 |
|--------|------|
| `> 0` | 买入 / 调仓至该仓位比例（`1.0` = 满仓，`0.30` = 三成仓） |
| `-1` | 卖出 / 清仓 |
| `0` | 不调仓（维持当前持股） |

> 绝大多数策略买入时发 `1.0`，**只有单边下跌策略发 `0.30`**（小仓位抄底，见第 9 节）。回测引擎 `run()` 按 `target = min(signal, 1.0)` 取目标仓位，再按目标市值差额调仓。
>
> 注意：信号值不能直接 `int()` 取整——`int(0.30) == 0` 会被误判成「不调仓」，自动交易 `compute_signal_detail()` 因此统一用 `raw > 0` / `raw < 0` 判方向。

> 范围说明：回测引擎共内置 **13 种**信号方法，其中 9 种作为 UI 预设（即下表）。海龟（`turtle`）、动量（`momentum`）、网格（`grid`）、漏斗建仓（`funnel`）为引擎内置但未列入预设，可通过 API 直接指定 `type` 调用。

## 策略总览

| # | 名称 | type | 适用场景 | 一句话逻辑 |
|---|------|------|---------|-----------|
| 1 | 双均线交叉 | `ma_cross` | 趋势跟踪 | 快慢均线金叉买、死叉卖 |
| 2 | MACD金叉死叉 | `macd` | 趋势跟踪 | MACD 金叉买，跌破 60 日线卖 |
| 3 | 布林带突破 | `bollinger` | 均值回归 | 跌破下轨买、突破上轨卖 |
| 4 | RSI超买超卖 | `rsi` | 超买超卖反转 | RSI 超卖回升买、超买回落卖 |
| 5 | KDJ随机指标 | `kdj` | 短线反转 | K/D 金叉买、死叉卖 |
| 6 | 单边上升策略 | `uptrend` | 单边上涨 | 均线多头 + 突破追涨 + 移动止损 |
| 7 | 震荡盘整策略 | `oscillation` | 箱体震荡 | 下轨/超卖买，上轨 + RSI 卖 |
| 8 | 上升回调策略 | `pullback` | 上升回调低吸 | 趋势过滤 + 回调锚点 + 分层止盈 |
| 9 | 单边下跌策略 | `downtrend` | 超跌反弹 | 超卖 + 创新低 + 收阳，小仓位抄底 |

> 其中 **6 上升回调 / 7 单边上升 / 8 震荡盘整 / 9 单边下跌** 同时被选股系统复用，但选股有**独立的初筛条件**与**略微收紧的入场判定**（例如选股版回调策略额外要求「缩量」），两者口径差异见文末对照表。

---

## 1. 双均线交叉（ma_cross）

| 参数 | 默认值 | 说明 |
|------|--------|------|
| fast | 5 | 快线周期 |
| slow | 20 | 慢线周期 |

- **买入**：快线上穿慢线（金叉）
- **卖出**：快线下穿慢线（死叉）

## 2. MACD金叉死叉（macd）

| 参数 | 默认值 | 说明 |
|------|--------|------|
| fast | 12 | 快线 EMA |
| slow | 26 | 慢线 EMA |
| signal_period | 9 | 信号线周期 |
| ma_filter | 60 | 60 日均线过滤器 |

- **买入**：股价站上 60 日均线，且 MACD 金叉（DIF 上穿 DEA）
- **卖出**：股价跌破 60 日均线（直接清仓离场）

## 3. 布林带突破（bollinger）

| 参数 | 默认值 | 说明 |
|------|--------|------|
| period | 20 | 周期 |
| std | 2.0 | 标准差倍数 |

- **买入**：收盘价跌破布林下轨（前一日在下轨上，今日跌破）
- **卖出**：收盘价突破布林上轨（前一日在上轨下，今日突破）

> 逻辑为均值回归：超跌（跌破下轨）买入、超涨（突破上轨）卖出。

## 4. RSI超买超卖（rsi）

| 参数 | 默认值 | 说明 |
|------|--------|------|
| period | 14 | RSI 周期 |
| oversold | 30 | 超卖线 |
| overbought | 70 | 超买线 |

- **买入**：RSI 从超卖区下方上穿超卖线（前一日 < 30，今日 ≥ 30）
- **卖出**：RSI 从超买区上方下穿超买线（前一日 > 70，今日 ≤ 70）

## 5. KDJ随机指标（kdj）

| 参数 | 默认值 | 说明 |
|------|--------|------|
| n | 9 | N 日 |
| k_period | 3 | K 周期 |
| d_period | 3 | D 周期 |

- **买入**：K 线上穿 D 线（金叉）
- **卖出**：K 线下穿 D 线（死叉）

## 6. 单边上升策略（uptrend）

| 参数 | 默认值 | 说明 |
|------|--------|------|
| fast | 5 | 买入均线周期 |
| trail_pct | 8 | 回撤止损 % |

- **前提**：MA_fast > MA10 > MA20（均线多头排列）
- **买入**（二选一）：
  - 通道1：站上 fast 日均线 + MACD 翻红（柱线由绿转红）→ 追买
  - 通道2：收盘价 > 过去 20 日最高收盘价，且当日涨幅 < 9.5% → 突破买入
- **卖出**（仅两条，其余一律持有）：
  - ① 从 20 日最高收盘价回撤超过 8%
  - ② 跌破 MA60

## 7. 震荡盘整策略（oscillation）

| 参数 | 默认值 | 说明 |
|------|--------|------|
| boll_period | 10 | 布林周期 |
| boll_std | 2.0 | 布林标准差倍数 |
| rsi_period | 14 | RSI 周期 |
| rsi_oversold | 30 | RSI 超卖线 |
| kdj_n / kdj_k / kdj_d | 9 / 3 / 3 | KDJ 参数 |
| j_oversold | 0 | J 超卖线 |

- **买入**（MA20 > MA60 趋势过滤，且三选二）：
  - ① 股价触及/跌破布林下轨
  - ② RSI 低于超卖线
  - ③ KDJ 的 J 值低于超卖线并拐头向上
- **卖出**（两条件同时满足才卖出）：
  - ① 股价触及布林上轨（close ≥ 上轨 × 0.99）
  - ② RSI > 50

## 8. 上升回调策略（pullback）

| 参数 | 默认值 | 说明 |
|------|--------|------|
| macd_fast / macd_slow / macd_signal | 12 / 26 / 9 | MACD 参数 |
| boll_period / boll_std | 20 / 2.0 | 布林带参数 |
| kdj_n / kdj_k / kdj_d | 9 / 3 / 3 | KDJ 参数 |
| rsi_period | 14 | RSI 周期 |
| rsi_low / rsi_high | 35 / 50 | RSI 回落区间 |
| j_turn | 40 | J 值拐头阈值 |
| mb_low / mb_high | 0.95 / 1.10 | 中轨锚点区间（-5% ~ +10%）|
| deviation_max | 0.20 | 偏离 MA20 上限 |
| position_max | 0.85 | 250 日区间位置上限 |
| ma60_up_min | 10 | MA60 20 日上涨天数下限 |
| loss_stop_pct | 3 | 早期止损 % |
| early_days / hold_days | 5 / 15 | 止损观察期 / 确认期（天）|
| trail_pct | 8 | 移动止损回撤 % |

- **买入**（绝对趋势 + 位置过滤 + 硬性前置 + 四组条件均为必选）：
  - 绝对趋势方向确认：过去 20 日 MA60 上涨天数 ≥ 10（否则拒绝，不参与 MA60 下跌的股票）
  - 价格位置过滤：当前价在 250 日区间位置 ≤ 85%（高位拒绝）
  - 硬性前置：偏离 MA20 ≤ 20%，且 close ≥ MA60
  - ① 趋势确认：MACD 的 DIFF > 0
  - ② 价格锚点（二选一）：中轨附近（-5%~+10%）或 跌破下轨 + 绿柱缩短
  - ③ 动能确认（二选一）：KDJ 的 J 值低位拐头 或 RSI 回落至 35~50
  - ④ 止跌确认：日线 MACD 绿柱缩短 或 日线 KDJ 金叉（回测 / 自动交易走日线近似；实时接口改用**真实 60 分钟 K 线**，见 8.1）
- **卖出**（分层状态机，按买入后天数）：
  - 买入后 5 天内：浮亏超 3% 立即止损
  - 6~15 天：站上 MA20 切换 8% 移动止损；15 天内仍未站上 MA20 直接清仓
  - 超过 15 天：-8% 移动止损 + 跌破 MA60 清仓

### 8.1 两套实现：日线近似 vs 真实 60 分钟

第 ④ 步「止跌确认」有两套实现，**入场口径不同**：

| 实现 | 第 ④ 步判定 | 调用方 |
|------|------------|--------|
| `signals_pullback` | 日线 MACD 绿柱缩短 或 日线 KDJ 金叉 | 回测 `/api/strategy/backtest`、自动交易 `compute_signal_detail()` |
| `check_pullback_signal` | 真实 60 分钟 K 线的 MACD 绿柱缩短 或 KDJ 金叉（`_pullback_min60_confirm()`） | 实时信号 `/api/strategy/pullback/signal/{code}` |

前 3 步与全部硬性前置条件（MA60 趋势方向、250 日位置、偏离度、MA60 之上、趋势确认、价格锚点、动能确认）两套完全一致，**只有第 ④ 步不同**。

60 分钟版更严格，因为真实盘中数据比日线近似多一层确认，所以同一标的可能出现「回测能买、实时不买」。若两者结果需要严格可比，请以同一实现为准。

实时接口支持自定义参数，优先级 `params` > `strategy_id` > 预设默认值：

```bash
# 用当前用户已保存的策略参数（需登录，且策略 type 必须是 pullback）
GET /api/strategy/pullback/signal/600519?strategy_id=3

# 直接传 JSON 参数（无需登录）
GET /api/strategy/pullback/signal/600519?params={"rsi_low":30,"rsi_high":45}
```

响应中的 `params_used` 字段回显本次实际生效的参数，便于核对。

## 9. 单边下跌策略（downtrend）

| 参数 | 默认值 | 说明 |
|------|--------|------|
| rsi_period | 14 | RSI 周期 |
| rsi_oversold | 20 | RSI 超卖线（极度超卖）|
| rsi_target | 50 | RSI 止盈目标 |
| new_low_window | 20 | 新低窗口 |
| position_size | 0.30 | 仓位比例（小仓位抄底）|

- **买入**（四条件同时满足，小仓位 30% 介入）：
  - ① RSI < 20（极度超卖）
  - ② 当日最低价创 20 日新低（盘中触及 20 日最低）
  - ③ MACD 绿柱缩短（今日绿柱 > 昨日绿柱，底背离）
  - ④ 今日收盘价 > 昨日收盘价（收阳止跌）
- **卖出**（任一满足即止盈）：
  - ① RSI 回升至 50 以上
  - ② 股价触及 MA5（close ≥ MA5）

---

# 行情选股（选股系统）

实现在 `backend/services/market_select.py`，入口在「选股分析 → 行情选股」页面（`/selection`）。

它和上面 9 个策略的关系是：**上面 9 个策略回答「什么时候买卖」，选股系统回答「今天该盯哪些票、用哪条策略去交易」**。选股不产生交易信号，只产出一份带策略标签与目标仓位的候选清单。

## 选股流程总览

```
大盘指数（上证指数 sh000001）
   │
   ├─ 第一段 大环境过滤
   │     跌超 1%  或  均线空头排列单边下跌  ──▶ 空仓，终止今日选股
   │
   ├─ 第二段 分轨制初筛（两条互斥轨道，互不共享条件）
   │     ├─ 顺势轨道（仓位 100%）──▶ 只找 上升回调、单边上升
   │     └─ 逆势轨道（仓位 ≤30%）──▶ 只找 震荡盘整、单边下跌
   │
   ├─ 第三段 轨道内逐级匹配（能低吸就不追涨，取首个命中）
   │
   └─ 第四段 无任何命中 ──▶ 输出空仓建议，终止
```

> **为什么分轨**：A 股是单边做多市场。如果只用一套「MA60 上涨」的全局初筛，那么「逆势抄底」类策略（震荡盘整、单边下跌）在初筛阶段就被过滤光了 —— 要求 MA60 连涨 10 天的股票，不可能同时处在 RSI<20、创 20 日新低的超卖状态。分轨后两条逻辑各走各的通道，并给逆势轨道加上仓位硬上限。

## 第一段 · 大环境过滤

读取大盘指数日线（默认**上证指数 `sh000001`**，腾讯优先、akshare 兜底），按以下顺序判定：

| 判定 | 结果 |
|------|------|
| 当日跌幅 > **1%** | **空仓**，终止今日选股 |
| MA5 < MA10 < MA20（均线空头排列）**且** MA20 低于 5 个交易日前的 MA20 **且** 收盘价 < MA20 | **空仓**，终止今日选股（明显的单边下跌趋势） |
| 收盘价 > MA20 且 MA5 > MA10 | 通过，标记为「**温和上涨**」 |
| 其余 | 通过，标记为「**震荡企稳**」 |

- 指数数据不可用时**不阻断流程**，但会在结果里标记 `available=false`，避免因数据故障被误判成「空仓」。
- 该段耗时不到 1 秒；只有通过后才会做后面的全市场扫描。

## 第二段 · 分轨制初筛

两条轨道**互斥且互不共享条件**（顺势要求 MA60 连涨、逆势要求 MA60 走平或向下，天然不重叠）。

| | **顺势轨道**（主力） | **逆势轨道**（小仓位试错） |
|---|---|---|
| **MA60** | 连续 **10 天上涨**（严格递增，逐日比较） | 走平或向下：MA60 ≤ 10 日前 MA60 × 1.01 |
| **价格位置** | 250 日区间位置 **< 85%** | — |
| **与均线关系** | 收盘价 **> MA60** | 收盘价 < MA60 × **0.8**（严重偏离）<br>**或** RSI 处于**历史低位** |
| **偏离度** | 偏离 MA20 **≤ 20%** | — |
| **可匹配策略** | 上升回调、单边上升 | 震荡盘整、单边下跌 |
| **仓位** | 100% | **≤ 30%** |

其中「**RSI 历史低位**」= 满足任一：

- RSI(14) < 30（绝对超卖）
- RSI(14) 处于近 250 日 RSI 区间的**下方 10% 分位**

**两轨共有**的候选池条件（在扫描前统一处理）：

- 非 ST
- 上市天数 > **60 天**（次新股跳过）
- 剔除创业板（`300` / `301` / `302`）、科创板（`688` / `689`）、北交所（`920` / `43` / `83` / `87`）
- 最后一根 K 线必须是**最新交易日**，否则视为停牌 / 长期无成交并剔除

## 第三段 · 四大策略匹配

对通过初筛的股票，在**所属轨道内**按优先级逐级判定，**取第一个命中的策略**。顺序严格遵循「**能低吸就不追涨**」：

### 顺势轨道

**① 上升回调策略（低吸，优先级最高）**

条件全部满足：

- **上升趋势**：MA20 > MA60
- **近期缩量回调**：近 5 日均量 < 前 5 日均量，**且** 收盘价 < 近 10 日最高收盘价（确实已从高点回落）
- **趋势确认**：MACD 的 DIFF > 0
- **动能确认**（二选一）：J 值 < 40 且今日 J > 昨日 J（低位拐头）**或** RSI 回落至 35 ~ 50
- **价格锚点**：股价在 MA20 的 **-5% ~ +3%** 区间内（`MA20×0.95 ≤ close ≤ MA20×1.03`），**或** 已跌破布林下轨

> 「高于 MA20 超过 5% 的票，必须跌到布林下轨才允许买入」这条规则，在本区间设定下是自动成立的：涨幅超 +5% 必然落在 -5%~+3% 区间之外，只能走「跌破布林下轨」这条路径。

**② 单边上升策略（追涨，低吸不满足时才启用）**

条件全部满足：

- **均线多头发散**：MA5 > MA10 > MA20 > MA60，且 MA5 与 MA20 的距离较前一日**扩大**
- **MACD 刚翻红**：柱线由 ≤0 转为 >0
- **触发**（二选一）：收盘价站上 MA5 **或** 突破过去 20 日最高收盘价
- **当日涨幅 < 9.5%**（涨停 / 接近涨停买不进）

### 逆势轨道

**③ 震荡盘整策略**

- **前提**：MA20 > MA60，且 MA20 近 10 日变动 < 2%（**均线走平**）
- **买点三选二**：① 触及布林下轨 ② RSI < 30 ③ J < 0 且拐头向上

**④ 单边下跌策略**

四条件**缺一不可**，且**仅分配 30% 仓位**：

- ① RSI < 20（极度超卖）
- ② 当日最低价创 20 日新低
- ③ MACD 绿柱缩短（今日绿柱 > 昨日绿柱）
- ④ 当日收阳（收盘价 > 昨日收盘价）

## 第四段 · 结果处理

- **无任何命中** → 直接输出**空仓建议**并终止，界面以红色告警展示原因
- **有命中** → 按「顺势轨道 → 逆势轨道」排序展示，每只标注轨道、策略、触发条件与目标仓位

## 选股口径 vs 回测口径

同名策略在两处的判定**并不完全相同** —— 选股是「单点快照」，回测是「带持仓状态的状态机」，且选股多了一层收紧：

| 策略 | 回测 / 自动交易（`signals_*`） | 选股（`_match_*`） |
|------|------------------------------|-------------------|
| 上升回调 | 四组条件（趋势 / 锚点 / 动能 / 止跌）+ 分层卖出状态机 | **额外要求「近期缩量」**；价格锚点收紧为 MA20 的 -5%~+3% |
| 单边上升 | 均线多头排列 + 双通道买入 | **额外要求「均线发散」**（MA5 与 MA20 距离扩大） |
| 震荡盘整 | MA20 > MA60 + 三选二 | **额外要求「MA20 走平」**（近 10 日变动 < 2%） |
| 单边下跌 | 四条件同时满足 | 一致 |

因此**回测能买、选股不一定选**；选股是更严格的一层过滤。

## 返回结构与统计

选股接口返回 `{action, message, market, results, stats}`：

- `action`：`select`（有标的）/ `empty`（空仓）
- `message`：空仓原因或完成说明
- `market`：大盘环境快照（指数点位、涨跌幅、MA5/10/20/60、判定标签与理由）
- `results`：命中标的，含 `track`（轨道）、`strategy`（策略）、`reason`（触发条件）、`position_size`（目标仓位）
- `stats`：候选数、剔除板块数、剔除 ST 数、停牌数、**取数失败数**、命中数、基准交易日、轨道与策略分布

> 统计里的**取数失败数**很重要：数据源限流时该值会飙升，此时结果是被截断的、不完整的，界面会亮黄灯提示。早期版本会把失败与「不符合条件」混为一谈，导致限流时静默返回近乎空的结果。

